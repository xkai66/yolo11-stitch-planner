# YOLO11n 玉米杂草检测：缝合计划 v0.1

> 日期：2026-08-30  
> 主线：基于轻量化 YOLO11n 模型的田间玉米杂草识别研究
> 状态：候选设计，尚未把新模块写入训练 YAML；网页中的参数/GFLOPs为估算，最终以本地实测为准。

## 1. 目标与边界

目标是围绕 YOLO11n 做一个可解释、可消融、能部署的轻量改良模型：

1. 以验证集 mAP50-95 为主指标，同时报告 mAP50、Precision、Recall、参数量、GFLOPs、单张/单帧延迟和模型大小。
2. 重点解决玉米苗期杂草小、边界模糊、颜色和背景相近造成的漏检，不把“模块越多”当成创新。
3. 目标参数区间 2.5–4.5M；优先 3.0–3.8M。若超过 4.5M，必须有明确的精度/召回收益和边缘设备速度证据。
4. 每次只改变一个可归因因素，先 5 轮初筛，再对入选者做 100 轮完整训练。新顶会模块没有本地结果时只能写“候选/待验证”。

## 2. 本地证据与缝合约定

当前训练工程的真实入口是 `C:\Users\xkai2\Desktop\训练集\yolov11\train.py`。它负责遍历 `ultralytics/cfg/models/11/*.yaml`、构建模型、训练、记录 CSV 和超时/OOM；当前代码没有 `--stitch` 参数，也没有解析 `a+b+c` 的函数。现有模型是直接在 YAML 中修改 backbone/head，例如：

- `yolo11-AFPN-P345.yaml`：保留原 backbone，head 直接换成 `Detect_AFPN_P345`，属于 Neck/多尺度融合路径改动。
- `yolo11-RCSOSA.yaml`：把多处 `C3k2` 换成 `RCSOSA`，同时影响 backbone 和 head 的特征变换。
- `yolo11-ELA-HSFPN-TADDH.yaml`：head 中使用 `ELA_HSFPN`，末端使用 `Detect_TADDH`，属于 Neck + Head 组合。

本计划中的 a+b+c 采用如下工程约定，定版后再实现真正的 YAML 生成器：

| 符号 | 区域 | 规则 |
|---|---|---|
| a | Backbone 算子 | 只替换一个指定 stage 的 `C3k2` 或在该 stage 外挂一个轻量 block；默认 P3/P4 附近，避免重复堆叠 |
| b | Neck/特征融合 | 只选择一个融合路径或一个尺度增强策略；默认优先 P3/P4，必要时再评估 P2 |
| c | Head/检测头 | 只替换检测头或头前的单个增强块；必须记录输出尺度、输入通道和接口 |

因此 `a+b+c` 不是把三个完整 YAML 生硬拼接，而是：以 base 的层索引和通道为骨架，按位置逐段替换，并在构建阶段做 shape、参数量和 FLOPs 检查。

## 3. 现有已验证池（A 组）

以下数字沿用现有实验摘要；本地最新 CSV 与早期摘要存在口径差异，最终排序需统一按同一批 5 轮结果复核。

| 编号 | 已验证模块 | 主要区域 | 用户当前摘要中的相对收益 | 处理建议 |
|---|---|---|---:|---|
| A1 | AFPN-P345 | Neck/Head 融合 | +0.0597 | 当前首选基线改进；优先作为 b |
| A2 | RCSOSA | Backbone + Head 特征变换 | +0.0325 | 作为 a 或轻量注意力对照 |
| A3 | AFPN-P2345 | Neck/Head 融合 | +0.0039 | 只在小目标 Recall 有同步提升时保留 |
| A4 | ELA-HSFPN-TADDH | Neck + Head | retry +0.0213 | 作为强组合对照，不与新 Neck 盲目叠加 |

本地 `summary_screen_20260829_004344.csv` 还显示 base `yolo11.yaml` 为 2.62408M 参数、5 轮 best mAP50-95 为 0.22902；这与此前摘要中的 2.59M/6.4GFLOPs 和收益数字不是同一统计口径，暂不把两套数字混写成论文结论。

## 4. 2025–2026 新模块候选池（B/C 组）

### B 组：优先做小规模适配

| ID | 模块/来源 | 可截取位置 | 论文公开参数 | YOLO11n 缝合增量 | 为什么适合小目标杂草 | 风险 |
|---|---|---|---|---|---|---|
| B1 | MobileNetV4 UIB/ExtraDW，ECCV 2024 | Backbone 的一个 P3/P4 `C3k2` | MNv4-Conv-S 整体 3.8M 参数、0.2G MACs；不是单块增量 | 约 +0.03–0.25M/块，估算，待实测 | UIB 将点卷积、深度卷积和可选扩展深度组合，ExtraDW 用低成本扩大感受野；适合保留纹理同时控制边缘端开销 | 需要把 stride、shortcut 和通道对齐；不能直接把整网替换进 YOLO11n |
| B2 | Mamba-Adaptor，CVPR 2025 | Backbone P4 或 Neck P3/P4 的单个 adaptor | 论文摘要未给单 adaptor 参数；待按实际通道测量 | 约 +0.05–0.35M/插入点，工程估算 | Adaptor-T 做可学习记忆选择，Adaptor-S 用多尺度空洞卷积；适合兼顾长程背景和杂草局部边界 | 状态空间实现、显存和推理算子兼容性需先做 dry-run |
| B3 | ConvNeur 的 global-local decoupling，CVPR 2026 | P4 Backbone 或 Neck 的单个 global/local block | 论文页未给 YOLO11n 单块参数；待实测 | 约 +0.08–0.40M/块，工程估算 | 把轻量全局记忆和局部细节分开，再用门控调制；有利于区分杂草边缘与玉米叶片背景 | 论文结构不是 YOLO 原生模块，需自行压缩为单块；缺少现成 YOLO 接口 |

### C 组：有价值但工程风险更高

| ID | 模块/来源 | 可截取位置 | 论文公开参数 | YOLO11n 缝合增量 | 为什么值得试 | 风险 |
|---|---|---|---|---|---|---|
| C1 | EfficientViM 的 HSM-SSD，CVPR 2025 | Backbone P4/P5 单块 | 论文页未给单块参数；待实测 | 约 +0.10–0.60M/块，工程估算 | 用隐藏状态混合和多阶段融合降低视觉 Mamba 成本，可能补充田间长条纹理和遮挡上下文 | 状态维度、CUDA/CPU实现会显著影响速度；不建议首轮堆在 P3 |
| C2 | WaveFormer 的 WPO，AAAI 2026 | Neck P3/P4 的频域/全局增强块 | AAAI 页面未报告单模块参数；待实测 | 约 +0.02–0.20M/块，工程估算 | WPO 用轻量频域操作保留高频细节并建立全局一致性，理论上适合模糊小杂草边缘 | FFT/频域算子可能增加端侧延迟；必须用真实设备测 FPS |
| C3 | CARE Transformer 的 decoupled dual interaction，CVPR 2025 | P3/P4 Neck 或 Backbone 单块 | 论文页未给单块参数；待实测 | 约 +0.05–0.30M/块，工程估算 | 将局部归纳偏置与长程依赖分开，再做双向交互，适合“叶片局部纹理 + 行间背景” | Transformer接口和显存开销不确定，作为 C 组备选而非第一批全量 |

### 相关但暂不列为主池

- CVPR 2026 workshop 的 P2 高分辨率特征整合直接针对极小目标，但属于 workshop 且会增加 P2 分支开销；若现有数据确实大量目标小于 P3 感受范围，再单独作为 Neck 消融，不与其他新模块首轮组合。
- AAAI 2026 SimROD 的 GGE 只有 4 个参数，但论文面向 RAW 图像绿通道增强；当前 MaizeSet 是标准 RGB，不能把它直接当作通用检测模块。
- MambaVision、Vision-MoR 等完整骨干/检测器参数明显超出 2.5–4.5M 目标，不纳入 YOLO11n 主池。

## 5. 三条主缝合路线

| 路线 | a Backbone | b Neck | c Head | 目的与判定 |
|---|---|---|---|---|
| A1 | base 或轻量 C3k2 | AFPN-P345 | RCSOSA 轻量版/原 Detect | 以现有最好 Neck 为主，观察注意力增益是否可叠加；若参数超过预算则 c 回退为 Detect |
| A2 | base | ELA-HSFPN | TADDH | 复现实有 retry 正收益，作为“强融合+强头”上限对照，不再增加第三个重模块 |
| A3 | UIB/ExtraDW（B1） | AFPN-P2345 或简化 P345 | base Detect | 验证高效骨干 + 浅层融合；若 AFPN-P2345 对小目标 Recall 无明显优势则改用 P345 |

新顶会替换路线建议：

1. `AFPN-P345 → C2/WaveFormer`：只替换 Neck，能回答“新版全局/频域增强是否优于已有 AFPN”，不要同时换 backbone/head。
2. `RCSOSA → B2/Mamba-Adaptor`：保持插入 stage、通道和重复数一致，比较注意力/状态空间算子的收益和速度。
3. `ELA-HSFPN-TADDH → B1 + base Detect`：把现有组合拆开，只保留轻量 UIB/ExtraDW，判断能否用更小参数获得接近收益。
4. `AFPN-P2345 → CVPR26 P2 备选`：仅在小目标统计证据充分时启用；P2 分支增加的 FLOPs、显存和延迟必须单独报告。

## 5.1 本轮实际执行的 5 个方案

下面 5 个方案只使用当前 Ultralytics 源码中已经存在、能够被 YAML 解析的模块。它们是“可执行方案”，不是把尚未实现的 WaveFormer/ConvNeur 直接冒充成可训练代码。mAP 与 FPS 是规划区间/相对目标，真实值由 5 轮初筛覆盖；参数与 GFLOPs也由 dry-run 覆盖。

| 方案 | a（Backbone） | b（Neck） | c（Head） | 预估 mAP50-95 | FPS目标（相对base） | 参数预估 | 一句话创新点 | 论文落点 |
|---|---|---|---|---:|---:|---:|---|---|
| S1-MM-AFPN345 | P4 单个 `C3k2_MobileMamba` | `AFPN-P345` | `Detect_AFPN_P345` 内置头 | 0.25–0.33 | 0.85–1.00× | 2.7–3.6M | 用轻量多感受野状态空间块补充上下文，再由 AFPN 保留多尺度细节 | 第4章 结构改进；第5章 a+b 消融 |
| S2-EV-AFPN345 | P4 单个 `C3k2_EfficientVIM` | `AFPN-P345` | `Detect_AFPN_P345` 内置头 | 0.24–0.32 | 0.85–1.00× | 2.7–3.6M | 用低成本隐藏状态混合增强遮挡场景，同时保留 AFPN 小目标路径 | 第4章 轻量骨干；第5章与 S1 横向比较 |
| S3-ELA-TADDH | 原始 `C3k2` | `ELA_HSFPN` | `Detect_TADDH` | 0.27–0.31 | 0.70–0.95× | 约3.01M（已有 retry 实测） | 用高频/多尺度融合配合自适应检测头改善边界和定位 | 第4章 Neck+Head；第5章强组合消融 |
| S4-MM-AFPN2345 | P4 单个 `C3k2_MobileMamba` | `AFPN-P2345` | `Detect_AFPN_P2345` 内置头 | 0.24–0.31 | 0.80–0.98× | 2.8–3.8M | 对比 P3–P5 与加入浅层 P2/P3 语义后的收益，验证小目标路径是否值得成本 | 第5章浅层融合消融 |
| S5-EV-TADDH | P4 单个 `C3k2_EfficientVIM` | 原始 PAN | `Detect_TADDH` | 0.24–0.31 | 0.80–0.98× | 2.8–3.8M | 将新型轻量骨干和现有检测头解耦，判断收益来自特征提取还是检测头 | 第5章模块归因与组合 |

说明：当前 base 的本地 5 轮 CSV 记录为 2.62408M、6.6 GFLOPs、best mAP50-95=0.22902；FPS 尚未有统一的端到端实测，因此上表用相对目标而不是虚构绝对 FPS。S3 的 3.014931M/0.28902来自 retry CSV，可作为已有证据，不等同于本轮新批次结果。

## 5.2 模块到底改了什么

```text
输入图像
   │
   ▼
[Backbone a]  提取边缘、纹理、语义；S1/S4替换P4一个C3k2为MobileMamba
   │           S2/S5替换P4一个C3k2为EfficientViM
   ▼
[Neck b]      把P3/P4/P5（或P2）融合成检测所需的多尺度特征
   │           AFPN-P345偏向稳定三尺度；AFPN-P2345偏向浅层小目标
   ▼
[Head c]      将特征变成类别、框位置和置信度；TADDH是检测头改造
   │
   ▼
检测结果：玉米 / 杂草
```

- `MobileMamba`：轻量多感受野视觉 Mamba。放在 P4 而不是所有 stage，是为了只增加一个中层上下文单元，避免像全网替换那样拖慢 FPS。
- `EfficientViM`：以 HSM-SSD 等隐藏状态混合降低视觉 Mamba 成本。放在 P4，既有一定语义上下文，又不直接压缩最敏感的 P3 小目标细节。
- `AFPN-P345`：三尺度特征融合，优先作为当前最佳 Neck 对照；它回答“融合路径改良是否有效”。
- `AFPN-P2345`：引入更浅层特征，重点回答“额外浅层信息是否真的改善幼苗/小杂草”，但可能带来噪声和计算量。
- `ELA_HSFPN`：高频/空间方向的融合增强；适合模糊边缘，但分支较多，速度风险比 AFPN 高。
- `TADDH`：改变检测头特征处理/定位方式；适合作为 Head 单独消融，不能把 Neck 与 Head 的收益混为一个“黑箱创新”。

## 5.3 如何判定“哪个改得好”

排序不采用单一“最高 mAP”偷换目标，而采用硬约束 + 主指标：

1. 先排除 build 失败、OOM、超时、数据未完整跑完 5 轮的记录。
2. 在相同数据划分、imgsz、batch、seed、设备和后处理设置下，主排序看验证集 mAP50-95；mAP50、Precision、Recall作为解释指标。
3. 计算 `ΔmAP = 模型 mAP50-95 − base mAP50-95`、`ΔFPS = 模型 FPS − base FPS`、`ΔParams` 和 `ΔGFLOPs`。
4. 满足 `ΔmAP > 0` 且 `FPS ≥ 0.95×base`、参数不超过 4.5M 的，优先进入 100 轮；若 FPS 小于 0.95×base，只有当 mAP 提升足够明显且部署目标允许，才保留为精度路线。
5. 100 轮后以 best mAP50-95 为主，检查验证曲线是否稳定、是否过拟合；最终 test 集只评估一次，不参与选择。

可用下面的简化决策：

```text
       mAP50-95 是否高于 base？
          /                 \
        否                   是
        │                     │
      淘汰       FPS是否≥0.95×base？
                            /       \
                          是         否
                          │           │
                    优先100轮     精度路线候选
```

## 5.4 消融怎么设

以 base 为 0 号组，至少形成以下可解释矩阵：

| 组 | a | b | c | 作用 |
|---|---|---|---|---|
| E0 | base | base | base | 基线 |
| E1 | MobileMamba(P4) | base | Detect | 只测新骨干 |
| E2 | EfficientViM(P4) | base | Detect | 只测另一种新骨干 |
| E3 | base | AFPN-P345 | AFPN头 | 只测现有最佳融合 |
| E4 | base | base | TADDH | 只测检测头 |
| E5 | MobileMamba(P4) | AFPN-P345 | AFPN头 | S1，a+b组合 |
| E6 | EfficientViM(P4) | AFPN-P345 | AFPN头 | S2，a+b组合 |
| E7 | base | ELA-HSFPN | TADDH | S3，已有强组合 |
| E8 | MobileMamba(P4) | AFPN-P2345 | AFPN头 | S4，浅层融合路线 |
| E9 | EfficientViM(P4) | base | TADDH | S5，骨干+头路线 |

第一轮不必一次训练完 E0–E9；本次先运行 E0 + S1–S5 做工程和方向初筛。若其中某个方案进入 100 轮，再补它所需的单模块 E1/E2/E4 消融，保证论文能解释增益来源。

## 6. 消融与执行顺序

1. **接口 dry-run**：每个新模块单独生成一个 YAML，检查 `nc=1`、P3/P4/P5 输出 shape、参数量、GFLOPs、forward 和 export。
2. **单模块 5 轮**：base、A 组强基线、B1/B2/B3、C1/C2/C3 逐个对比，固定数据划分、imgsz、batch、seed、workers 和 AMP。
3. **初筛规则**：同一 epoch 的 mAP50-95 高于 base 且未出现明显速度/参数失控，才进入组合；“前五轮更高”仅是候选信号，不等于 100 轮有效。
4. **组合消融**：先 `a`，再 `a+b`，最后 `a+b+c`；每个新增模块都要有“去掉它”的对照。
5. **100 轮终训**：只保留 1–3 个最有希望的组合，使用独立 test 集做最终一次评估；测试集不参与筛选和调参。

## 7. 参数、速度和毕业工作量

- 最终模型必须用同一输入尺寸和同一设备实测 parameters、GFLOPs、显存峰值、预处理/推理/后处理耗时、FPS 和模型文件大小。
- 论文不写“估算参数”为结果；网页中的估算只用于排除明显过大的组合。
- 工作量适中：1 个 base、4 个已验证/复现实验、3–5 个新模块单项、2–4 个组合消融、1–3 个 100 轮终训，再加可视化和边缘端测试，足够形成“问题—模块—消融—部署”的毕业论文主线。
- 不建议同时使用两个全局建模模块，也不建议把 AFPN、HSFPN、P2、Transformer/Mamba 一次性全部堆上去；这样无法解释增益来源，也更容易超时或超显存。

### 7.1 超时与断点记录规则

当前批量入口的正常阈值是每个完整 epoch 平均 180 秒。Windows 首轮可能包含数据加载、CUDA kernel 初始化和编译预热，因此首轮允许最多 360 秒；从第二个完整 epoch 开始，按已完成 epoch 的平均耗时判断。超时记录为 `skipped_timeout`，不能写成“已训练”、不能参与“超过基准”的有用模块统计，也不能自动进入 100 轮。OOM、结构解析失败、用户中断各自保留独立状态。这样既避免把启动抖动误判为模型性能问题，也保留异常的可追溯性。

## 8. 参考来源

- [MobileNetV4，ECCV 2024](https://www.ecva.net/papers/eccv_2024/papers_ECCV/papers/05647.pdf)
- [Mamba-Adaptor，CVPR 2025](https://openaccess.thecvf.com/content/CVPR2025/html/Xie_Mamba-Adaptor_State_Space_Model_Adaptor_for_Visual_Recognition_CVPR_2025_paper.html)
- [EfficientViM，CVPR 2025](https://openaccess.thecvf.com/content/CVPR2025/html/Lee_Efficient_Vision_Mamba_with_Hidden_State_Mixer_based_State_CVPR_2025_paper.html)
- [CARE Transformer，CVPR 2025](https://openaccess.thecvf.com/content/CVPR2025/html/Zhou_CARE_Transformer_Mobile-Friendly_Linear_Visual_Transformer_via_Decoupled_Dual_Interaction_CVPR_2025_paper.html)
- [ConvNeur：Efficiency Follows Global-Local Decoupling，CVPR 2026](https://openaccess.thecvf.com/content/CVPR2026/html/Yang_Efficiency_Follows_Global-Local_Decoupling_CVPR_2026_paper.html)
- [WaveFormer，AAAI 2026](https://ojs.aaai.org/index.php/AAAI/article/view/39737)
- [CVPR 2026 workshop：P2 Feature Integration](https://openaccess.thecvf.com/content/CVPR2026W/AERO-HPR/html/S_Enhancing_Aerial_Pedestrian_Detection_via_High-Resolution_P2_Feature_Integration_in_CVPRW_2026_paper.html)
